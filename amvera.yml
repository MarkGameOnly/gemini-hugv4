meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt

run:
  scriptName: launch.py
  command: python launch.py
  persistenceMount: /data
  containerPort: "8000"
